{
  "name": "factory-traceability-suite",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "azure-function"
  ],
  "scripts": {
    "dev": "npm --workspace frontend run dev",
    "build": "npm --workspace frontend run build",
    "typecheck": "npm --workspace frontend run typecheck",
    "function:start": "npm --workspace azure-function run start"
  }
}
